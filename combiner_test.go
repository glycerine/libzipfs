package libzipfs

import (
	"testing"

	cv "github.com/glycerine/goconvey/convey"
)

func TestCombinerAndSplitterAreInverses(t *testing.T) {

	cv.Convey("we should be able to mount a zipfile image in the tmp dir", t, func() {
		dir := "/tmp" // "" => use system tmp dir
	})
}
